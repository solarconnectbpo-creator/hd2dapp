/**
 * Fallback + TypeScript resolution. Metro prefers `LidarMapComponent.web.tsx` / `.native.tsx` when present.
 */
export {
  LidarMapComponent,
  type LidarMapComponentProps,
} from "./LidarMapComponent.native";
export { default } from "./LidarMapComponent.native";
